[project]
name = "KittyKaboom"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["kivy", "plyer", "pyjnius"]

[tool.uv.scripts]
ballroller = "source.main:main"
